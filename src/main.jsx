import React from "react";
import ReactDOM from "react-dom/client";
import MeyarDashboard from "./MeyarDashboard.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MeyarDashboard />
  </React.StrictMode>
);
